#page_action_links
  = link_to 'New Message', new_message_path

#messages
  - @messages.each do |message|
    .message_body{ id: message.id }= render :partial => 'message', :locals => { :message => message }

%br
